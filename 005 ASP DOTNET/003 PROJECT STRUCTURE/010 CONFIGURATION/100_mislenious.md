#### Why "__" Used? : ConnectionStrings__DefaultConnection
`__` means:

```text
:
```

**Example:**

```text
ConnectionStrings__DefaultConnection
```

same as:

```json
ConnectionStrings:DefaultConnection
```